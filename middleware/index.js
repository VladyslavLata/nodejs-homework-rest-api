const validateBody = require("./validateBodyContacts");
const isValidid = require("./isValidid");
const authenticate = require("./authenticate");

module.exports = {
  validateBody,
  isValidid,
  authenticate,
};
